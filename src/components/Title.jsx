function Title({textSize}) {
    return (
        <h1 className={`font-pub text-[#f1d5a7] drop-shadow-[0_4px_4px_rgba(0,0,0,0.8)] cursor-default tracking-widest uppercase ${textSize}`}>
            Shut The Box
        </h1>
    )
}
  
export default Title